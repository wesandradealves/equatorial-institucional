// Variablesw

import { fonts } from '@/assets/scss/fonts';
import { colors } from '@/assets/scss/colors';
import { breakpoints } from '@/assets/scss/breakpoints';
import { spacing } from '@/assets/scss/spacing';

export const _fonts = fonts;
export const _colors = colors;
export const _breakpoints = breakpoints;
export const _spacing = spacing;